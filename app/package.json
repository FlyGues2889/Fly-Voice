{
  "name": "fly-voice",
  "version": "0.1.0",
  "description": "A notice displayer",
  "main": "main.js",
  "icon": "app/assets/appIcon.ico",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packager": "electron-packager . FlyVoice --win --out ./out --icon=assets/appIcon.ico --overwrite"
  },
  "author": "FlyGues",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.2.1",
    "@electron/packager": "^18.3.6"
  },
  "dependencies": {
    "electron": "^33.2.1",
    "@electron/packager": "^18.3.6"
  }
}
